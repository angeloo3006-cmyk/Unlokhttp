# NetScope desktop app

This directory contains the Tauri v2 and React frontend. See the repository
root [README](../README.md) for setup instructions, including the C++ sidecar.

Useful commands:

```powershell
npm install
npm run build
npm run sidecar:build
npm run tauri dev
```
